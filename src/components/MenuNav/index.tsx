import NavItem from "../NavItem";
import * as S from "./styles/MenuNavStyles";

const MenuNav = () => {
    return (
        <S.NavContainer>
            <NavItem />
        </S.NavContainer>
    );
};

export default MenuNav;
